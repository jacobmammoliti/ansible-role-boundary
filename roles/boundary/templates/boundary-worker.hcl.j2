listener "tcp" {
    purpose = "proxy"
    tls_disable = {{ boundary_tls_disable }}
}

worker {
  name = "{{ ansible_hostname }}"
  description = "Boundary controller node"

  # Workers must be able to reach controllers on :9202
  controllers = {{ boundary_controller_list }}

  public_addr = "{{ boundary_client_addr }}"
}